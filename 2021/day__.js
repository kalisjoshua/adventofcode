const example = `

`

module.exports = (input, {report}) => {
  input = input.trim().split(/\n/)

  const partOne = input

  const partTwo = input

  // report('Part one', partOne)
  // report('Part two', partTwo)
}
